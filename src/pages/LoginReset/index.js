import React from 'react';
import styles from './LoginReset.module.css';

const LoginReset = () => {
  return (
    <section>
      <h1>Login Reset</h1>
    </section>
  );
};

export default LoginReset;
